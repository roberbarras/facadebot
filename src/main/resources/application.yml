server:
  port: ${PORT:8090}
kafka:
  server: 'localhost:9092'
  receiveMessageTopic: 'org.telegram.bot.receivemessage'
  sendMessageTopic: 'org.telegram.bot.sendmessage'
logging:
  level:
    root: off
    org:
      telegram:
        bot:
          facadebot: debug